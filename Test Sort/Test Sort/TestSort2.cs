namespace TestSort
{
    /*
    public enum UserMenuChoice
    {
        PerformAnotherSort,
        Quit
    }
    private void PerformSortingForUser()
    {
        // print something like "Enter an array size"
        // use Console.ReadLine to get what they type
        // use int.TryParse(...) to see if you can make it an integer
        // declare your int[] myArray = new int[ValueUserTypedIn];
        // randomize it, sort it, display it
    }
    private UserMenuChoice HandleMainMenu()
    {
        // display the main menu
        // read what the user types
        // if it's a '1', return UserMenuChoice.PerformAnotherSort
        // otherwise, return UserMenuChoice.Quit
    }
   // public static void Main()
    {
        PerformSortingForUser();
        UserMenuChoice choice = HandleMainMenu();
        while (choice != UserMenuChoice.Quit)
        {
            if (choice == UserMenuChoice.PerformAnotherSort)
                PerformSortingForUser();
            choice = HandleMainMenu();
        }
    }
    */
}